import React from "react";

const toolbar = (props) => {
  return (
    <React.Fragment>
      <header>
        <div>Menu</div>
        <div>Logo</div>
        <nav>...</nav>
      </header>
    </React.Fragment>
  );
};

export default toolbar;
